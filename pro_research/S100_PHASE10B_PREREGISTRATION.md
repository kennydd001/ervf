# Phase 10B preregistration
1. Real-weight cold stream over every Mamba FP8 in/out matrix.
2. Bit-exact output check for every matrix.
3. Freeze at most two candidates with >=5% cold-stream speedup.
4. Fresh-process smoke then full A/C/C/B on Phase-9 current-map alpha=0.0003.
5. Full promotion >=0.25 ms/token with exact tokens and stable timing.
6. Component GB/s is never an S100 claim.
