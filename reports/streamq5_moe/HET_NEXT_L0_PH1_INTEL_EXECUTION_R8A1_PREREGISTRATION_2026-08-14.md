# PH1 Intel execution R8A1 — terminal-state verifier repair

Date: 2026-08-14. Status: immutable open authorization source; physical execution remains forbidden until an independent source GO.

R8A1 changes no payload, arithmetic, kernel, device, resource, threshold, or physical lifecycle in the frozen R7A delegate. It supersedes the verifier-defective R8A package and binds its independent NO-GO audit SHA `cae07159709049ef452ac8c49aeedfc9e930964940958ff9bf284936d565dd7a`. The sole methodological repair is a fresh authorization/output namespace plus exact terminal-state adjudication.

The only physical command is `.venv\Scripts\python.exe -I -B <absolute R8A1 runner> --ack PH1_INTEL_EXECUTION_R8A1_AFTER_R8P8_PASS_AND_TERMINAL_AUDIT_GO`. Before any filesystem mutation, payload read, compiler, OpenCL load, or device call, the runner validates the native Windows command vector, venv/base identities, direct-entry primitives, its open lock, committed R8P8 PASS18/18 and independent PASS14/14 bundle, the complete frozen R7D/PASS9/PASS7/PASS18 chain, R7A-verifier absence, and exact immutable R7D1/R8P6 failures. R7D1 authorization is never called. The unchanged R7A authorization/package verifier and `execute_authorized` are delegated only after these gates; its deterministic CPU package preparation therefore remains unchanged and occurs only inside the frozen physical runner.

Exactly one fresh attempt is allowed. A committed bundle is valid only when authorization/provenance and the frozen numerical verifier pass. A positive has every frozen check true. A committed negative must have exact negative result status, a complete device/post-device bundle, valid authorization/package provenance, and at least one explicit frozen physical gate false; an authorization/provenance/precheck failure is never relabeled as a valid committed negative.

A delegated nonzero without a commit is evidence-valid only when exactly one new inherited R7A failure bundle exists and one canonical R8A1 wrapper summary binds its path, SHA-256, exact schema/cardinality/bytes, disposition, return code, and device-opened flag. Missing, multiple, extra, oversized, wrong-kind, wrong-disposition, wrong-hash, bare return 3, or success-without-commit states are invalid. Early outer failures are retained as bounded infrastructure-negative evidence but never pass the component. Positive, committed-negative, correlated-delegated-failure, early-failure, and invalid protocol states are mutually exclusive.

The standalone verifier does not import the candidate runner. It independently rehashes all locks/evidence, recomputes stored runner invocation and its own live direct `.venv -I -B` invocation from primitives, runs nonvacuous mutations, checks the exact terminal topology, then invokes the frozen numerical verifier only for a valid committed bundle. Its output is create-new and atomic. Claim boundary remains one real expert/input Intel correctness component only; no performance, model, multi-device, or breakthrough claim.
