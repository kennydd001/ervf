# HET-NEXT-L0 PH1 Intel execution R8A5 preregistration

Date: 2026-08-14. Status: frozen source package; physical execution remains closed pending an independent source audit. This is a namespace/lifecycle repair only. The R7A compiled kernel, Q5 records, CPU oracle, controls, numerical gates, resource limits, device identity, buffer contract, launch contract and scientific claim are unchanged.

## Purpose and prior negative

R8A4 exited before `configure()`, payload construction, OpenCL loading or device access. Its wrapper attempted an ancestry-depth lookup that resolved to a module without the requested attribute. The immutable diagnosis is `HET_NEXT_L0_PH1_INTEL_EXECUTION_R8A4_PREDEVICE_BINDING_FAILURE_DIAGNOSIS_2026-08-14.md`, SHA-256 `d88ff5fd76e11757d7d53acf7279bc52897b9b74169414daee3ce18cd8bc6b21`; the independent R8A4 source audit is SHA-256 `4dc0c0a1f3e411f78f81ef667baf6e00e94f73204f0ed9b3d1794b0f300a7438`. All R8A4 output, wrapper-failure, backend-failure, quarantine and verification paths were absent after that protocol-negative attempt.

R8A5 repairs only module resolution and bounded early evidence. It imports by exact full module name:

- the frozen historical/auth contract `run_het_next_l0_ph1_intel_execution_r8a.py`, SHA-256 `552a7f08f83f2ba2ce3da29581029dfdd79e86fbb75faeb71356965073228f15`;
- the frozen physical implementation `run_het_next_l0_ph1_intel_execution_r7a.py`, SHA-256 `01fa21266137335494de2d21adba11f45fe83ff95f660d90cef7acc389c1cb04`;
- the frozen numerical verifier `verify_het_next_l0_ph1_intel_execution_r7a.py`, SHA-256 `18b64765469e38c5211d28afe586e0a559e97f6e2110f09f54c4f58d9c38dd88`.

The current runner and verifier contain no revision-depth traversal. An AST gate requires those two explicit runner imports and rejects imports of R8A1--R8A4 wrappers and multi-level attribute chains rooted at the forbidden ancestry aliases.

## Pre-device authorization order

The only valid command is the exact Windows native vector `.venv\\Scripts\\python.exe -I -B <absolute R8A5 runner> --ack PH1_INTEL_EXECUTION_R8A5_AFTER_R8P8_PASS_AND_EXPLICIT_BINDING_AUDIT_GO`, observed through both `GetCommandLineW`/`CommandLineToArgvW` and Python vectors. The venv launcher, base executable, prefixes, flags, launcher SHA and `pyvenv.cfg` SHA remain frozen as in R8P8/R8A4.

Before any filesystem remapping, payload read, compiler/OpenCL call or device call, the runner requires: exact invocation; all current and R8A--R8A4 terminal paths absent; no R8A5 temporary path; the current AST gate; exact current lock; exact module `__file__` and SHA; required callable/constant attributes; the immutable R8P8, R7D and two historical failure predicates; and exact R8A4 audit/diagnosis hashes. The module-resolution sentinel is read-only. The explicit R7A import only defines constants/classes and does not construct the CPU package or call OpenCL. `physical.authorize()` is invoked only after the sentinel and is a read-only contract/package-manifest check.

Wrong argv, wrong ACK, non-direct entry or an already-used/nonclean topology returns 3 without filesystem mutation. With a correct direct invocation and clean topology, a resolution or authorization exception produces exactly one bounded, create-new R8A5 `early_outer_failure` record with `device_opened=false`, stage `predevice_resolution_or_authorization`, the primary exception and traceback. This is infrastructure-negative and never a scientific/device negative.

## Physical delegation and lifecycle

Only after authorization, R8A5 remaps the frozen R7A output/failure/quarantine roots to fresh R8A5 paths and calls `R7A.execute_authorized()` exactly once. No R7D1 authorization function or failed namespace is reused. The frozen R7A implementation still performs the 16 GiB start-RAM gate, package build, pre-device resource gate, OpenCL execution, retained resource samples, output/counter/control gates, atomic result bundle, cleanup and bounded failure evidence.

A committed R7A-format bundle is terminal. A delegated nonzero without a commit is acceptable evidence only when exactly one new backend attempt contains exactly one bounded canonical R7A failure whose schema is valid and `device_opened=true`; R8A5 writes an exact correlated wrapper summary. Bare nonzero, zero-without-commit, missing/multiple/extra evidence and all mismatches are invalid protocol. Early infrastructure evidence remains terminal-invalid. Existing/partial paths block authorization; there is no retry.

## Independent adjudication

The standalone verifier does not import the candidate runner. It independently freezes paths, invocation, hashes, lock and authorization extension. It validates the exact three-file committed bundle or recursively exact wrapper/backend failure trees. The terminal classes are mutually exclusive:

1. `positive`: all frozen R7A numerical, protocol, ownership, cleanup and resource gates pass;
2. `allowed_device_negative`: only the already frozen output-stage or counter scientific gate is false and every protected gate is true;
3. `correlated_device_negative`: exact wrapper plus exact `device_opened=true` backend evidence;
4. `early_invalid` or `invalid`: never valid scientific evidence.

The verifier retains the R8A4 31-case production terminal matrix under injected temporary roots: valid correlated; all missing/multiple/extra/orphan/temp/quarantine cases; kind/status/stage/disposition/hash/correlation/device mutations; bare return; success without commit; mixed terminals; early infrastructure; committed positive; both allowed negatives; and four protected-gate negatives. It also independently mutates all invocation primitives. Only `positive` returns exit 0; allowed negatives return structured evidence and exit 3.

## Claim boundary

The claim remains only one real expert/input Intel correctness component. There is no throughput, full-layer, model-quality, cross-device or heterogeneous-execution claim. This freeze authorizes no execution. Independent source audit is required before the single physical attempt.
