# PORT80B-T0Q5-R5 — execution/provenance repair

R5 preserves the immutable T0Q5-R3 scientific protocol and thresholds and supersedes the R4 NO-GO implementation. Execution is closed pending source audit; prompt lock and output directory must remain absent.

R5 requirements:

- use `port80b_t0q5r5_transaction.py` for every multi-file bundle, verifier artifact and failure artifact; create-new names, fsync files/directories, exact complete-marker verification and recover/quarantine all incomplete states;
- an already verified committed bank is reusable by a Q5 retry; recovery must never quarantine a valid committed bank;
- finalize result bytes exactly once before computing transaction journal hashes;
- reference verifier independently verifies canonical prompt replay/disjointness, exact schema/finiteness, router direct/second and top-k semantics, actual residual add, shared gate-first multiplication, complete MLP identity, caches, source identities/runtime/resources and commits a pass artifact;
- Q5 cannot start without the exact hash-bound reference pass artifact;
- Q5 verifier imports no runner/codec, independently rereads shard and requantizes all 1,539 records, validates every canonical manifest core row/offset/digest, decodes selected records, reruns full graphs, recomputes the code-mutation selector, safe rejections, unsafe raw arrays and metrics;
- count bank, manifest, commit, raw, result, commit, verification and retained failure bytes in the 1.10-GiB gate; sample resources after final serialization and cleanup;
- standalone tokenizer-only prompt verifier and self-hash-bound preflight execute real prompt checks and actual TEMP transaction simulation, while never loading model, reading shard payload, forwarding, building bank or initializing GPU.

R5 implementation may mechanically reuse R4 numerical functions only after hashing the R4 source and must override its transaction, retry, verification and artifact behaviors. A source GO authorizes only generation of a canonical prompt lock and CPU no-forward preflight in a new locked revision.
