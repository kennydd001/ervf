# Phase 18R artifact handoff

The standalone overlay is named `ervf_s100_lightning_phase18r_dualgraph.zip`.

It intentionally uses new `s100_lightning18r_*` filenames and does not overwrite the locally patched Phase-18 files. Run it against the completed Phase-17 worktree. Hardware results publish to `agent/s100-lightning-phase18r-dualgraph-hardware`.
