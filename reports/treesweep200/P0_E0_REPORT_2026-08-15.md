# P0 + E0 — identiteit vastgelegd, N1–N5 gereproduceerd

Datum: 2026-08-15
Registry: NEMOTRON_TREESWEEP_200_ROOFLINE_V2 (`reports/treesweep200/`)
Verdict: **`gate_passed` voor P0 en E0.** Alle vier de poorten gehaald; alle acht
geïmporteerde claims geclassificeerd als `reproduced`. De exact-efficiency
track (E1–E6) is nu geopend.
Preregistratie: `P0_E0_PREREGISTRATION_2026-08-15.md` (bevroren vóór uitvoering)

## 1. Identiteit (P0)

Target is **Nemotron 3.5 Lightning 30B-A3B NVFP4**, lokale map
`models/nemotron_3_5_lightning_v35`, architectuur `NemotronHForCausalLM`:
48 lagen (23 MoE, 23 Mamba-2, 6 GQA-attention), hidden 2688, 128 routed experts
met top-k 6, 1 shared expert, ReLU²-MLP (intermediate 1856), GQA 32 q-heads /
2 kv-heads × head_dim 128, één NextN/MTP-laag (`num_nextn_predict_layers: 1`).
Alle identiteitsbestanden en runtime-modules gehasht in
`P0_IDENTITY_MANIFEST.json`; de MTP-wiring en v35-layout zijn via hun eigen
locks (`s10a_wiring_resolution.json`, `n2r_v35_layout.json`) overerfde,
onafhankelijk geverifieerde feiten.

Baseline-verwijzing (G-P0-B1): `n7b_cached_decode.json` —
27,743 / 26,200 / 21,699 / 18,424 tok/s bij ctx 0 / 32K / 131K / 262.100,
in S10A al binnen 2,6% gereproduceerd.

## 2. E0 — eigen roofline en bytevloeren

Eigen streaming-roofline (eigen kernel, 256 MiB, `float4`, 10 reps, p50):
**330,5 GB/s** tegen 338,4 geïmporteerd (−2,3%, poort ≤ 10%).

Eigen bytemodel, onafhankelijk van N5: bytetellingen uit de
safetensors-shard-headers (resident = totaal − backbone-expertbank − MTP −
embed), expertlezing per token = 23 × 6 × (up-record + 9,31% × down-record,
met 9,31% uit de S2-census), KV = 6 × 2 × ctx × 256 B (FP8).

| context | eigen totaal | eigen vloer | geïmporteerd | Δ |
|---:|---:|---:|---:|---:|
| 0 | 1.987,7 MiB | 6,31 ms | 6,05 ms | +4,3% |
| 4.096 | 1.999,7 MiB | 6,34 ms | — | — |
| 32.768 | 2.083,7 MiB | 6,61 ms | — | — |
| 131.072 | 2.371,7 MiB | 7,52 ms | — | — |
| 262.100 | 2.755,6 MiB | 8,74 ms | 8,43 ms | +3,7% |

Beide binnen de 10%-poort (G-E0-F1). Het verschil is verklaard: mijn telling
neemt de sparse down-lezing mee (S2's gemeten 9,31%) en telt de resident shell
uit headers in plaats van uit geladen tensors.

Plafonds bij de eigen roofline: **158,6 tok/s bij ctx 0** en **114,4 tok/s bij
262K**. 50 tok/s vereist 31,6% van de roofline bij ctx 0 en 42,8% bij 262K.

## 3. Classificatie van de import

| claim | classificatie | grond |
|---|---|---|
| N5 roofline 338,4 GB/s | `reproduced` | eigen meting 330,5 (−2,3%) |
| N5 vloer ctx 0, 6,05 ms | `reproduced` | eigen 6,31 ms (+4,3%) |
| N5 vloer 262K, 8,43 ms | `reproduced` | eigen 8,74 ms (+3,7%) |
| N1 graph-winst 23,7% | `reproduced` | in-repo 36/36 onafhankelijk geverifieerd |
| N2 gather 8,192 ms @4,3 GB/s | `reproduced` | idem |
| N4 attention 47,2 GB/s, R²=0,9964 | `reproduced` | idem |
| N3 low-rank prefilter gesloten | `reproduced` | idem |
| N5 kritieke GEMV 81,4 GB/s | `reproduced` | y2r1-artefact + y1y2-verificatie |

De import had de status `USER_MEASURED_UNVERIFIED` omdat het pack de in-repo
verificatie-infrastructuur niet kende; die bestaat en is hier gehasht en
hercontroleerd. Alle bronnen staan in `E0_N1_N5_EVIDENCE_MANIFEST.json`.

## 4. Poorten

| poort | uitkomst |
|---|:--|
| G-P0-I1 identiteit compleet + gehasht | ✅ |
| G-P0-B1 baseline vastgelegd | ✅ |
| G-E0-R1 roofline binnen 10% | ✅ (−2,3%) |
| G-E0-F1 vloeren binnen 10% | ✅ (+4,3% / +3,7%) |

Verifier: **alle checks geslaagd, `VERIFIED`**. Drie verifier-fouten in de
eerste versie (een geketende vergelijking, een float/int-truncatie, een
telfout in het aantal claims) zijn als verifier-bugs hersteld; de
runner-artefacten stonden er los van en zijn ongemoeid gebleven.

Protected manifest na deze fase: zie
`reports/lightningstream_nemotron/protected_verification_after_p0e0.json`.

## 5. Claim boundary

De bytevloeren zijn een eigen herberekening uit checkpoint-headers plus gemeten
sparsiteit, gedeeld door een eigen gemeten roofline: harde bovengrenzen voor
elke semantiek-behoudende implementatie, geen runtime-metingen. De
classificaties steunen op in-repo geverifieerde artefacten met vastgelegde
hashes. Geen optimalisatie, geen doorvoerclaim, geen kwaliteitsclaim.

## 6. Artefacten

`P0_E0_PREREGISTRATION_2026-08-15.md` ·
`scripts/treesweep200/p0e0_identity_roofline.py` · `P0_IDENTITY_MANIFEST.json` ·
`E0_N1_N5_EVIDENCE_MANIFEST.json` · `E0_ROOFLINE_REPRODUCTION.json` ·
`scripts/treesweep200/e0_independent_verify.py` ·
`e0_independent_verification.json`
