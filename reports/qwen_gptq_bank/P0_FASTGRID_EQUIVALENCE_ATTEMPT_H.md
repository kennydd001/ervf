# Pure GPTQ fast-grid equivalence — attempt H lock

Locked after G passed and before H outputs. H revisits only the previously failed fast-grid attempt C with the diagnosed correction: every division uses the upstream CUDA `maxq` tensor, and `xmin1`/`xmax1` are materialized separately from the same 80 Python-defined shrink values. All reductions remain over 128 contiguous columns. Same 30 matrices and zero-mismatch rule. Failure leaves G authoritative; pass may accelerate only subsequent layers.
