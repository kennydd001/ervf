# PH1 Intel execution R8A2 — final terminal adjudicator

Date: 2026-08-14. Status: immutable open source package; no physical execution before independent source GO. This revision binds R8A1 audit SHA `7aec53fa62ddb8412e1364499f8c39326cd6fc2dd49451395399618304635287` and changes no payload, arithmetic, kernel, threshold, device, or frozen R7A lifecycle.

The sole physical command is `.venv\Scripts\python.exe -I -B <absolute R8A2 runner> --ack PH1_INTEL_EXECUTION_R8A2_AFTER_R8P8_PASS_AND_TERMINAL_AUDIT_GO`. All R8A1 live identity, R8P8 PASS18/PASS14, R7D/PASS9/PASS7/PASS18, historical-failure, R7A-verifier-absence, one-attempt, and preauthorization gates remain exact. R7D1 authorization is never called. The unchanged R7A package preparation and physical delegate run only after authorization and use fresh R8A2 paths.

The verifier independently exhausts the entire fresh R8A2 family topology. Exactly one terminal class is possible: positive committed bundle; allowed device-output committed negative; correlated post-device delegated failure; early infrastructure evidence; or invalid. Only the first three are terminal-valid, and only positive passes the component.

An allowed committed negative has exact `intel_execution_negative`/`positive=false`, valid authorization and immutable package provenance, a complete post-device result/bundle, and exact R7A gate schema. Only `stages` (bitwise output digest) and/or `counters` may be false. Controls, identity, compiled binary identity, ledger order, ownership, resource samples, allocations, writes, initialization, pointer arguments, launch, finish/read, release/cleanup, extensions, forbidden-call proof, and resources must all be true. Corresponding frozen numerical-verifier false names are restricted to `positive_schema`, `runner_gates`, plus `oracle_outputs` for `stages` and `counters` for `counters`; every other numerical check must be true. Thus authorization, provenance, precheck, protocol, lifecycle, identity, ledger, ownership, allocation, write, launch, read, release, cleanup, and resource failures cannot be reclassified as scientific/device negatives.

A correlated delegated negative requires exactly one canonical R8A2 wrapper summary, exactly one independently valid inherited R7A failure bundle, nonzero delegated return, byte/SHA/path/schema/cardinality/disposition correlation, and `device_opened=true`. Early/predevice infrastructure evidence has `terminal_valid=false`. Bare return, success without commit, missing/extra/multiple files or directories, orphan/temp/quarantine state, wrong kind/status/disposition/hash/device flag, or any mixed terminal topology is invalid.

The standalone verifier imports no candidate runner. Its production adjudicator is exercised by nonvacuous source-frozen mutations covering positive, each allowed negative, every protected gate, early failure, correlated failure, bare/missing/extra/multiple/incorrect correlation, and topology collisions. Claim boundary remains one real expert/input Intel correctness component only; no performance, model, multi-device, or breakthrough claim.
