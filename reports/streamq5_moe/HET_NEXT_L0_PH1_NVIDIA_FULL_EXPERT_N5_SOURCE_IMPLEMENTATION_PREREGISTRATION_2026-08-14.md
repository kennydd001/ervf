# PH1 NVIDIA N5 source freeze

Execution is closed. N5 binds N4 audit SHA `98f1965a7cc817b9ce4fe6a8e8ddd475b98b2cb62f92d2921e3e102fc404c320`. No preflight, scientific payload, compiler, or device call is authorized.

N5 freezes the exact Qwen3-Coder-Next shard path in common, verifier, and direct locks with a wrong-model mutation gate. Static preflight treats D2, shard, CPU stage safetensors, and LUT as stat-only scientific payloads; a real guarded-read mutation must be blocked while payload bytes remain zero. Only the exact absolute `-I -B` fixture-child subprocess is allowed. Independent output integrity is separated from expected equality so a stages/counters-only numerical negative remains valid evidence while every protocol/resource/lifecycle gate must pass. Prior ABI, ownership, requested/presented controls, bounded lifecycle, and direct-provenance gates remain frozen.
