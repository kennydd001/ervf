# PH1 Intel execution R8A3 — exhaustive backend-tree repair

Date: 2026-08-14. Status: immutable open source package; physical execution forbidden until independent source GO. R8A3 binds R8A2 audit SHA `148ef9cd0cfcb345f43ebe3449e3801776037bbc8d174e4cb130976576590296` and changes no physical payload, CPU preparation, arithmetic, kernel, threshold, device, resource, or frozen R7A execution semantics.

The exact one-attempt command is `.venv\Scripts\python.exe -I -B <absolute R8A3 runner> --ack PH1_INTEL_EXECUTION_R8A3_AFTER_R8P8_PASS_AND_TREE_AUDIT_GO`. R8P8, R7D/PASS9/PASS7/PASS18, runtime identity, historical failure, clean topology, R7A-verifier absence, and frozen R7A authorization/delegation gates remain unchanged under fresh R8A3 paths.

The R8A2 terminal rules remain exact: only `stages` and/or `counters` may be false in a valid committed device-output negative; every other frozen gate must be true. Early infrastructure evidence is not terminal-valid. A correlated delegated negative requires `device_opened=true`.

R8A3 additionally enumerates the entire inherited backend failure root recursively. It must contain exactly one direct attempt directory and exactly one file, the canonical `failure.json`, with no root file, nested/extra directory, extra/hidden file, temp, orphan, or other entry. The wrapper root has the same exact two-entry shape. Its independent verifier uses a TEMP filesystem and calls its actual production `failure_terminal()`, topology scanner, and reconstruct function for: valid correlated device-opened negative; missing backend; multiple attempts; extra attempt file; root orphan; extra directory; wrong kind; wrong SHA/correlation; wrong disposition; early infrastructure; and bare nonzero/success-without-commit. Only the valid correlated baseline is terminal-valid. The verifier imports no candidate runner.

Claim boundary remains one real expert/input Intel correctness component only; no performance, model, multi-device, or breakthrough claim.
