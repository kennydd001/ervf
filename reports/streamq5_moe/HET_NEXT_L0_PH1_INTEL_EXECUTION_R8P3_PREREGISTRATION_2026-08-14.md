# PH1 Intel execution R8P3 — current-helper transaction and static-boundary repair

Date: 2026-08-14  
Status: immutable closed preregistration; no execution authorization.

R8P3 supersedes the frozen R8P2 NO-GO and binds its independent audit, SHA-256 `9cb07a4bb35c335afb0be388c67167944e414c44e3d37685027b838b3d0c5be5`. The R8P2 dual Windows launcher/base identity, exact native/original/application vectors, runtime provenance, 16 GiB start-RAM gate, RECORD verification, frozen CPU preparation, 22 controls, five BF16 stage hashes, R7D1 failure and R8P1 protocol-negative evidence remain unchanged.

R8P3 adds three bounded repairs only:

1. The production R8P3 `atomic_create`, bundle publication, verification, quarantine, stale cleanup and failure writer are exercised through the actual functions in TEMP. Exact nonempty keys cover clean commit, unchanged repeat rejection, stale cleanup, pre-link failure, post-link unlink interruption and recovery, interrupted partial publication quarantine, and immutable committed bytes. The independent verifier implements and exercises its own writer and topology mutations. Result adjudication requires exact key sets and every Boolean true; empty, missing or extra dictionaries fail.
2. A genuine source AST/callgraph gate covers the candidate and its frozen inherited CPU-only path. It rejects compiler, model, OpenCL, CUDA and device imports/calls, proves the inherited R8 CPU bootstrap gate, and permits only kernel32/shell32 WinDLL calls used for command-line evidence. The independent verifier builds an injectable live dual-identity row and rejects mutations of native/original/application vectors, launcher/base identities, flags, trampoline, extra args, script and direct-entry state.
3. Exact claim boundary: a later source GO may authorize CPU-only reading of the already frozen CPU preparation slice. It does not authorize a model forward, compiler, OpenCL, CUDA, GPU or other device action.

Invalid application argv/token returns before filesystem mutation. A correct-token internal failure produces bounded create-new evidence. R8P3 is closed/PENDING; no preflight, payload read, compiler or device action is authorized by this freeze.
