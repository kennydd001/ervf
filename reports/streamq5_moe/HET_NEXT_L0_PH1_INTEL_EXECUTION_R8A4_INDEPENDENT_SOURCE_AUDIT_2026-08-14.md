# PH1 Intel execution R8A4 — independent frozen-source audit

Date: 2026-08-14  
Scope: read-only audit of the exact frozen runner, standalone verifier, preregistration, lock, inherited provenance, production terminal adjudicator, and 31-case temporary-filesystem matrix. No preflight, payload, compiler, OpenCL, or device call was made.

## Verdict

**GO for exactly one physical R8A4 attempt with the frozen token and `.venv -I -B` command, followed by the frozen standalone verifier.**

R8A4 closes both R8A3 blockers. Its temporary matrix invokes the actual current production `topology()` and `failure_terminal()` paths; `failure_terminal()` invokes the actual current recursive `exact_tree()` and `reconstruct()`. The matrix has exactly 31 named cases, requires every expected classification true, redirects all current topology globals, and restores all nine globals in `finally`. The physical runner and inherited R7A scientific computation remain unchanged.

This is execution authorization only. It is not a positive scientific result; that depends on the physical artifact and independent verifier produced by the one attempt.

## Frozen package verified

- runner: `scripts/streamq5_moe/run_het_next_l0_ph1_intel_execution_r8a4.py`, SHA-256 `672977892c1e714b7d6d218ac44e5e31499176ddf2781b4f2d9fcd26b65bc593`, 7,475 bytes;
- standalone verifier: `scripts/streamq5_moe/verify_het_next_l0_ph1_intel_execution_r8a4.py`, SHA-256 `d4100383bdac7bd759ff0b9f6833d42eddb49e33eabb59c83031f290e755ba86`, 18,617 bytes;
- preregistration: `reports/streamq5_moe/HET_NEXT_L0_PH1_INTEL_EXECUTION_R8A4_PREREGISTRATION_2026-08-14.md`, SHA-256 `aadea422e26e80f6ed66b032823dd461defac789f453245b1e2533f459f9977e`, 1,814 bytes;
- open lock: `reports/streamq5_moe/het_next_l0_ph1_intel_execution_r8a4_lock.json`, SHA-256 `b5da1d58a7ed5bdf2b59b4ae37262d62d348e8bdf061c81ad203bac01acb213c`, 4,572 bytes;
- bound R8A3 audit: SHA-256 `49131e25d27e2272a19f5890d11221b7d0e06ae90294c148d34e9d58ebff9131`;
- authorization token: `PH1_INTEL_EXECUTION_R8A4_AFTER_R8P8_PASS_AND_TOPOLOGY_AUDIT_GO`;
- fresh R8A4 output, inherited failure/quarantine, wrapper failure/quarantine, verifier output, and in-progress paths were absent at audit time.

## Runner and scientific invariance

The R8A4 runner is a namespace/authorization wrapper around the frozen R8A3→R8A2→R8A1→R7A chain. Before physical work it checks:

- the exact native and Python `.venv\Scripts\python.exe -I -B` invocation;
- current direct-entry primitives and pinned venv/base interpreter hashes;
- exact open lock, token, and one-attempt flag;
- the R8A3 audit and complete inherited hash chain;
- committed R8P8 and R7D/PASS9/PASS7/PASS18 contracts;
- exact immutable R7D1 and R8P6 failures;
- R7A-verifier absence and clean current/ancestor output topology.

It then obtains the frozen R7A authorization, adds only the R8A4 authorization extension, redirects every fresh result/failure/quarantine path, and invokes the unchanged physical executor. It never calls R7D1 authorization. No payload, Q5 codec, CPU preparation, arithmetic, kernel, threshold, resource, launch, output, or cleanup logic changed.

## Production terminal matrix

The verifier imports no R8A4 candidate runner. It independently binds the runner/hash chain and uses current implementations of:

- `topology()` with injectable `FAMILY_PARENT` and `FAMILY_PREFIX`;
- `production_terminal()`;
- `failure_terminal()`;
- recursive `exact_tree()`;
- `reconstruct()` and `correlated()`.

The TEMP harness redirects exactly nine globals—family parent, family prefix, lock, output, wrapper failure, backend failure, wrapper quarantine, backend quarantine, and verifier result—and restores all nine in `finally`.

Its exact 31 cases cover:

1. valid correlated post-device baseline;
2. missing/multiple wrapper attempts;
3. wrapper attempt extra file, wrapper root orphan, and wrapper extra directory;
4. backend root orphan, backend attempt extra file, missing backend, and multiple backend attempts;
5. current-family in-progress path;
6. quarantine collision;
7. wrong inherited kind, status, disposition, device false, and device wrong type;
8. wrong wrapper stage, correlation flag, and inherited hash;
9. bare nonzero, success without commit, mixed output/failure, and early infrastructure;
10. committed positive;
11. allowed `stages` negative and allowed `counters` negative;
12. forbidden precheck, protocol, lifecycle, and resource negatives.

The harness requires exactly 31 keys and all expected classifications true. The retained frozen R8A2 adjudicator mutation suite additionally exercises all 16 protected false gates, not only the four representative production-terminal fixtures.

## Terminal and claim boundary

- Only `positive`, `allowed_device_negative`, and `correlated_device_negative` are terminal-valid.
- Only `positive` makes the component pass and returns verifier exit code zero.
- A committed negative may have only `stages` and/or `counters` false, with the exact corresponding independent numerical false-set; every authorization, provenance, identity, protocol, ledger, ownership, allocation, write, launch/read, cleanup/release, extension, forbidden-call, and resource gate must remain true.
- Early/pre-device infrastructure evidence is terminal-invalid.
- A correlated delegated negative requires a canonical exact tree and `device_opened=true` on both wrapper and inherited evidence.
- Claim scope remains one official real expert/input Intel correctness component. There is no performance, full-model, multi-device, industrial, or breakthrough claim.

## Authorized sequence

Only the frozen command is authorized:

```powershell
& '.\.venv\Scripts\python.exe' -I -B 'C:\Users\de_do\Documents\ChatGPT\New project\scripts\streamq5_moe\run_het_next_l0_ph1_intel_execution_r8a4.py' --ack PH1_INTEL_EXECUTION_R8A4_AFTER_R8P8_PASS_AND_TOPOLOGY_AUDIT_GO
```

Do not retry or alter the command. After that single attempt, run the exact frozen standalone verifier under the same `.venv -I -B` runtime and adjudicate the immutable result/failure evidence. This source audit performed neither command.
