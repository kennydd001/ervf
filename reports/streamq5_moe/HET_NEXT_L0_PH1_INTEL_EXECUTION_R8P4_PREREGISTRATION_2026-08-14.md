# PH1 Intel execution R8P4 — bounded early-failure writer repair

Date: 2026-08-14  
Status: immutable closed preregistration; no execution authorization.

R8P4 supersedes the frozen R8P3 NO-GO and binds its independent audit SHA-256 `9aec9abc77a790f2eb4ef4685d84891b79c28cf00fc5c082e91d90099587fb85`. It changes only the early-failure evidence lifecycle. The dual Windows launcher/base identity, exact vectors, runtime/RECORD/RAM gates, frozen CPU preparation, 22 controls, five stage hashes, current transaction suite, AST/callgraph boundary and claim boundary are unchanged.

The current R8P4 failure writer accepts an injectable root and create function. Its exact TEMP suite calls that production writer and requires five nonempty Boolean outcomes: bounded exact-schema baseline; a second unique attempt without overwrite; pre-link failure with no empty attempt or temp; post-link temp-unlink interruption recovered to one bounded canonical evidence file and no temp; and preservation of the original primary exception with separately retained secondary writer error. The main exception boundary always re-raises the primary exception.

The independent verifier reimplements the failure writer and TEMP suite, requires the exact stored failure-simulation key set and all values true, and rejects empty, missing, extra and false simulation dictionaries.

Invalid application argv/token returns before filesystem mutation. A correct-token failure may write only bounded create-new evidence. A later source GO may authorize CPU-only reading of the already frozen preparation slice; model forward, compiler, OpenCL, CUDA and device actions remain forbidden. R8P4 is closed/PENDING and may not be run before independent source audit.
