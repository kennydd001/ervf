# R12-D2 full-stage diagnostic

Diagnostic only. For four prompts and fresh lengths 1..16, retain final-position input_norm, GDN, post_norm, direct official router logits/weights/IDs, experts, shared, shared_gate and layer output, plus full conv/recurrent cache. Retain corresponding whole tensors at every position. Prompt 1 whole and prefix-3 repeats retain the same complete stage/cache set. Independently recompute per-stage BF16/integer equality metrics, direct tuple/second-call evidence, tie sets, exact raw schema, finiteness, source/provenance/resource identities. Interpret only as same-length nondeterminism, GDN divergence, post-norm divergence, router divergence, expert/shared divergence, cache divergence or layer-output divergence. No thresholds, pass, manual MoE, Q5, bank, P4 or GPU claim.
