You are reviewing a small concurrent cache used by an inference engine. The cache has fifty-two slots per layer and each token selects eight experts. Explain why checking residency must happen before any insertion for the current token. Then write a short Python function that preserves stable route order while grouping repeated expert identifiers. Include one test where experts four through seven repeat in the next token.

Now solve this arithmetic problem carefully. A four-token verifier has a budget of 61.538 milliseconds. Projection, attention, routing, reduction, and the language-model head consume 60.095 milliseconds. Compute the remaining allowance and explain which measurements must include transfer latency. Do not round intermediate values.

Finally, summarize the design decision. Native NVFP4 computes a broad shortlist, exact ERVF reranks only those rows, and a bounded expert cache services routed feed-forward work. Distinguish measured kernel time from an end-to-end claim. State what evidence would falsify the design, including low cache hit rate, excessive host traffic, shortlist recall below one, or a mismatch between target and draft tokenization. Prefer a direct conclusion over optimistic wording.
